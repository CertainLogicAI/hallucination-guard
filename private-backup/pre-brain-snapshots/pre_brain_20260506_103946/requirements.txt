# CertainLogic Workspace Dependencies
# Generated: 2026-05-04
# Verified against all .py files in workspace (excludes archive/, skills-publish/, .build_data/)

# Brain API Server
fastapi>=0.104.0
pydantic>=2.0.0
uvicorn[standard]>=0.24.0

# HTTP Client
requests>=2.31.0

# Testing
pytest>=7.4.0

# Note: All other dependencies are Python stdlib (os, sys, json, re, datetime, etc.)
# Node dependencies (package.json): gray-matter, openai, redis, yaml
# These are for OpenClaw internals, not Python workspace
